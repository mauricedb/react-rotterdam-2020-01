import React from "react";
import { PacmanLoader } from "react-spinners";

const Loading = () => {
  return (
    <div style={{ height: 50 }}>
      <PacmanLoader />
    </div>
  );
};

export default Loading;
